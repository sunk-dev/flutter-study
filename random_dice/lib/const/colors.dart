//반복적으로 사용하는 상수들을 정리하기 위한 별도의 폴더 const를 만듬

import 'dart:ui';

import 'package:flutter/material.dart';

const backgroungColor=Color(0xFF0E0E0E);
const primaryColot=Colors.white;
final secondaryColor=Colors.grey[600]; //const 선언이 가능하지만 600이라는 키값을 입력하면 런타임에 색상이 계산되서 const사용 불가

